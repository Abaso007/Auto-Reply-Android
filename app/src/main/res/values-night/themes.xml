<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for DARK mode -->
    <style name="Theme.WaReply" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_500</item>
        <item name="colorSecondaryVariant">@color/blue_900</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/blue_700</item>
        <!-- IMPORTANT: Make status bar icons LIGHT in dark mode (white on blue background) -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <!-- Force action bar colors to always use blue -->
        <item name="actionBarStyle">@style/Theme.WaReply.ActionBar</item>
        <item name="colorPrimaryDark">@color/blue_700</item>
        <!-- Text colors for better visibility in dark mode -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/gray_500</item>
        <!-- Material text colors -->
        <item name="colorOnSurface">@color/white</item>
        <item name="colorOnBackground">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Custom ActionBar style to force blue color in all themes -->
    <style name="Theme.WaReply.ActionBar" parent="Widget.MaterialComponents.ActionBar.Primary">
        <item name="background">@color/blue_500</item>
        <item name="titleTextColor">@color/white</item>
        <item name="subtitleTextColor">@color/white</item>
    </style>

    <style name="Theme.WaReply.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/blue_700</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <style name="Theme.WaReply.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
    </style>

    <style name="Theme.WaReply.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
